
# Оценка населения по открытым цифровым данным (Population Estimation ML)

Этот проект представляет собой ML-инструмент для оценки численности населения на уровне отдельных зданий на основе открытых геоданных OSM (OpenStreetMap).

Ключевая идея: мы используем метод Transfer Learning (перенос обучения). Модель обучается на качественных данных одного региона (Свердловская область), где есть точная привязка "Здание -> Население", и затем применяется для предсказания населения в другом регионе (Пермский край), используя данные OSM.


## Цель и задачи

**Цель:** разработать алгоритм, который позволяет создать детальную карту плотности населения (до дома) для территорий, где доступна только общая статистика (по районам) и геометрия зданий.

**Задачи:**

- собрать и очистить разнородные данные (XLSX, GeoJSON, OSM)

- обучить модель машинного обучения (RandomForest) находить зависимость между параметрами здания (площадь, этажность) и количеством жителей

- сгенерировать карту населения Перми на уровне домов

- валидировать результат, сравнив агрегированные предсказания с официальной статистикой по районам

## Логика и Архитектура

**Схема работы:**

```
OSM здания
    ↓
Очистка и признаки
    ↓
Сопоставление с населением
    ↓
Обучение ML
    ↓
Расчёт населения по зоне
    ↓
GeoJSON / карта
```
```
graph TD
    A[Свердловск XLSX] -->|ETL| B(Обучающая выборка)
    B -->|Обучение| C[ML Модель: RandomForest]
    
    D[Пермь OSM Здания] -->|Очистка & Feature Eng| E(Предикторы: Area, Levels)
    C -->|Предсказание| F[Сырой прогноз по Перми]
    
    G[Пермь Статистика Точки] -->|Сумма| H[Коэффициент Нормализации]
    F -->|Нормализация| I[Финальная карта населения Перми]
    
    I -->|Сравнение по районам| J{Валидация}
```
    
**Почему мы обучаем на Свердловской области, а предсказываем Пермь?**

Это подход *Domain Adaptation*.
- **Свердловск:** у нас есть "Истина" (Ground Truth) на уровне конкретных зданий. Это идеальные данные для обучения
- **Пермь:** у нас есть только точки (центроиды кварталов/районов) с населением. Обучаться на точках нельзя (они не привязаны к геометрии дома)
- **Гипотеза:** архитектура и плотность заселения в Перми и Екатеринбурге схожи. Правила, выученные в одном городе, будут работать в другом

## Исходные данные

Все сырые данные находятся в папке **data/raw/** и **data/osm/**.

- sverdlovsk_buildings.xlsx: обучающий датасет. Содержит координаты, атрибуты здания и кол-во жителей (INHAB).

- perm_population_points.xlsx: данные по Перми. Содержит координаты точек (центры кварталов) и население. Используется для нормализации и валидации.

- perm_geojson_800mb.geojson: выгрузка OpenStreetMap по Пермскому краю (здания, геометрия).

## 📂 Структура проекта

```
population-estimation/
│
├─ data/
│   ├─ raw/                 # Исходные XLSX
│   ├─ osm/                 # OSM данные (geojson)
│   └─ processed/           # Результаты работы скриптов (очищенные данные)
│
├─ models/
│   └─ population_model.pkl # Сохраненная обученная модель
│
├─ scripts/
│   ├─ 01_prepare_sverdlovsk.py           # Подготовка обучающего датасета
│   ├─ 02_prepare_perm_points.py          # Конвертация точек Перми в GeoJSON
│   ├─ 03_load_and_clean_osm_buildings.py # Фильтрация "мусора" из OSM
│   ├─ 05_match_population_to_buildings.py# Dasymetric mapping (для валидации)
│   ├─ 06_build_training_dataset.py       # Feature Engineering
│   ├─ 07_train_model.py                  # Обучение RandomForest
│   └─ 08_estimate_population_for_zone.py # Предсказание, Нормализация, Валидация
│
├─ requirements.txt         # Библиотеки
└─ README.md                # Этот файл
```

##  🚀 Установка и Запуск

**1. Клонирование и окружение**
```
git clone <repo_url>
cd population-estimation

# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate
# Активация (Mac/Linux)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

*Основные зависимости:* pandas, geopandas, scikit-learn, osmnx, shapely, openpyxl.

**2. Запуск пайплайна (пошагово)**

Скрипты пронумерованы в порядке выполнения.

2.1. Подготовка данных Свердловска:
```
python scripts/01_prepare_sverdlovsk.py
```
Логи:
```
Загрузка XLSX...
Всего строк: 42403
После фильтра координат и INHAB: 42391
После формирования area_total: 42391
После фильтрации малых площадей: 42334
Загрузка границы Свердловской области...
После фильтрации по региону: 42312
Сохранение GeoJSON...
Сумма населения: 3659097.7984962407
Диапазон площадей (area_total): 8.0 - 277624.2
Диапазон этажей (levels): 1.0 - 42.0
Готово: data\processed\sverdlovsk_buildings.geojson
```

2.2. Подготовка точек Перми:

```
python scripts/02_prepare_perm_points.py
```

Логи:
```
Загрузка XLSX...
Сохранение GeoJSON.
```

2.3. Очистка OSM (Пермь):

*Критически важный этап. Фильтрует промзоны и некорректную геометрию.*

```
python scripts/03_load_and_clean_osm_buildings_local.py
```

Логи:
```
Загрузка точек населения...
Загрузка OSM (фильтрация по bbox при чтении)...
Объектов в зоне: 513029
После фильтра площади (10 - 15000 кв.м): 177401
После фильтра жилых: 65690
Готово: data\osm\perm_buildings_clean.geojson
```

2.4. Создание валидационного набора (Dasymetric Mapping):

*Распределяет население точек по ближайшим домам (для сравнения в конце).*

```
python scripts/05_match_population_to_buildings.py
```

Логи:
```
Загрузка зданий...
Всего зданий: 65690
Загрузка точек населения...
Всего точек населения: 6482
Распределение населения по зданиям...
Готово!
Жилых зданий с населением: 20882
Суммарное население: 1,126,152
```

2.5. Сборка датасета для обучения:

*Генерирует признаки (area, levels, total_floor_area).*

```
python scripts/06_build_training_dataset.py
```
Логи:
```
Загрузка данных Свердловской области...
Training dataset готов: data\processed\training_buildings.geojson
Примеров для обучения: 42311
```

2.6. Обучение модели:

*Обучает RandomForestRegressor. Выводит метрики качества (R2, MAE).*

```
python scripts/07_train_model.py
```
Логи:
```
Загрузка датасета...
Обучение на признаках: ['area', 'levels', 'total_floor_area']
Размер выборки: Train=33848, Test=8463
Запуск RandomForest...

Расчет метрик...
----------------------------------------
МЕТРИКА    | TRAIN      | TEST
----------------------------------------
R2 Score   | 0.887      | 0.895
MAE        | 16.44      | 20.20
----------------------------------------
Баланс Train/Test в норме.
```

2.7. Предсказание и валидация:
*Генерирует финальный GeoJSON и таблицу ошибок по районам.*

```
python scripts/08_estimate_population_for_zone.py
```
Логи:
```
Загрузка зданий OSM...
Целевое население (Пермь): 1,131,285
Загрузка модели и прогноз...
Сырая сумма ML: 2,686,589 -> Коэфф: 0.4211
Итоговая сумма ML: 1,131,285
```

##  Описание этапов (Pipeline)

**Feature Engineering (Признаки)**

Для каждого здания мы рассчитываем:

1. **Area** (площадь основания): из геометрии полигона.

2. **Levels** (этажность): берем из тега building:levels. Пропуски заполняем: 1 этаж для house, 5 этажей для apartments (или медианой).

3. **Total Floor Area** (общая площадь): Area * Levels. Самый сильный предиктор.

**Нормализация (Post-processing)**

Модель, обученная на одном городе, может ошибаться в абсолютных числах (Domain Shift).

- *Пример:* модель насчитала 2.6 млн жителей, а в реальности — 1.1 млн.

- *Решение:* мы применяем коэффициент k = Real_Pop / Predicted_Pop ко всем домам. Это сохраняет распределение плотности, но корректирует масштаб.

## ⚠️ Трудности и Решения

**Проблема "50 миллионов жителей"**

На ранних этапах модель предсказывала население Перми в 50+ млн человек.

- *Причина:* в данных OSM есть огромные полигоны (территории заводов, парков, коттеджных поселков), ошибочно помеченные как жилые здания (residential). Модель видела площадь 100 000 м² и "селила" туда тысячи людей.

- *Решение:* внедрен жесткий фильтр в скриптах 03 и 08. Удаляются все полигоны площадью > 15 000 м².

**Проблема "призраков на окраинах"**

При валидации выяснилось, что модель недооценивает население спальных районов на 40-50%.

- *Причина:* в OSM много объектов на окраинах (СНТ, дачи, промзоны), которые модель считает жилыми. Нормализация "размазывает" людей по этим объектам, отнимая их у реальных многоэтажек.

- *Решение:* частично решено через фильтрацию по площади, но требует более глубокого анализа типов зданий (landuse).

## 📊 Результаты и валидация

**Метрики модели (на тесте Свердловской обл.)**

```
Train R2: 0.887
Test R2: 0.895 (нет переобучения)
MAE (средняя ошибка): ~20 человек.
```

**Валидация на Перми (по районам)**

Мы сравнили предсказания модели с официальной статистикой.

| Район	| Ошибка (%)	| Интерпретация
|---|---|---|
| Ленинский (Центр)	| -12%	| Отлично. Модель хорошо поняла структуру центра
| Орджоникидзевский	| -7%	| Отлично. Окраинный район с понятной застройкой
| Свердловский	| -48%	| Недооценка. Люди "утекли" в пригород при нормализации
| Индустриальный	| -49%	| Недооценка

*Вывод:* модель верно определяет структуру расселения (где густо, где пусто), но абсолютные значения в спальных районах занижены из-за шума в данных OSM на периферии города.

## Направления для улучшения

- Маскирование территории: использовать слои landuse из OSM (industrial, commercial, forest), чтобы принудительно обнулять население в нежилых зонах.

- Кластеризация: обучать отдельные модели для "частного сектора" и "высоток".

##

**Авторы:** [Мельниченко Ирина, Михолап Вячеслав, Огай Владислав]

**Дата:** декабрь 2025
